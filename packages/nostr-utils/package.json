{
  "name": "@arcadecity/nostr-utils",
  "version": "0.0.1",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "description": "Nostr utils",
  "license": "CC0-1.0",
  "scripts": {
    "build": "tsup src/index.ts --format esm,cjs --dts --external react-native",
    "dev": "tsup src/index.ts --format esm,cjs --watch --dts --external react-native",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "tsup": "^6.0.1",
    "typescript": "4.7.4"
  },
  "dependencies": {
    "@scure/bip32": "1.1.0",
    "@scure/bip39": "1.1.0"
  }
}
