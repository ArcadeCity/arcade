{
  "name": "@arcadecity/nostr-utils",
  "version": "0.0.1",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "description": "Nostr utils",
  "license": "CC0-1.0",
  "private": true,
  "scripts": {
    "build": "tsup src/index.ts --format esm,cjs --dts",
    "dev": "tsup src/index.ts --format esm,cjs --watch --dts",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "tsup": "^6.0.1",
    "typescript": "4.7.4"
  }
}
